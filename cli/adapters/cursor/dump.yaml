site: cursor
name: dump
description: Dump the DOM and Accessibility tree of Cursor for reverse-engineering
domain: localhost
strategy: ui
browser: true

columns: [action, files]

pipeline:
  - evaluate: |
      (async () => {
        const dom = document.body.innerHTML;
        return [{ action: 'Dom extraction finished', files: 'DOM content available via pipeline' }];
      })()
