site: yollomi
name: try-on
description: "Yollomi AI try-on"
domain: yollomi.com
strategy: cookie
browser: true

columns: [status, detail]

pipeline:
  - navigate: https://yollomi.com
  - wait: 2
  - evaluate: |
      (function() {
        return [{ status: 'info', detail: 'Yollomi try-on requires cookie-authenticated API POST + file download chain. Use the TypeScript adapter for full functionality.' }];
      })()
